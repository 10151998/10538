<%= form_for(@challenge_message) do |f| %>
  <% if @challenge_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@challenge_message.errors.count, "error") %> prohibited this challenge_message from being saved:</h2>

      <ul>
      <% @challenge_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :datetime %><br />
    <%= f.datetime_select :datetime %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div><%= f.hidden_field :receiver_team_id, :value => @receiver_team_id %></div>
  <div><%= f.hidden_field :message_type, :value => @message_type %></div>
  <div class="actions">
    <%= f.submit "send" %>
  </div>
<% end %>
