<%= form_for(@team_point) do |f| %>
  <% if @team_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@team_point.errors.count, "error") %> prohibited this team_point from being saved:</h2>

      <ul>
      <% @team_point.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :team_id %><br />
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :point_of_week %><br />
    <%= f.number_field :point_of_week %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
