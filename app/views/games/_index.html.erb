<h1>Listing games</h1>

<%= form_tag('/user_game_preferences') do %>

  <% @games.each do |game| %>
    <div><%= check_box_tag "game_ids[]", game.id %>
    <%= game.name %></div>
  <% end %>

  <%= submit_tag 'Listing Location' %>
<% end %>
